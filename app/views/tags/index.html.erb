<h1>All Tags</h1>
<ul id="tags">
  <% @tags.each do |tag| %>
    <li><%= link_to tag.name, tag_path(tag) %>
      <span><%= link_to 'Delete', tag_path(tag), method: :delete, data: {confirm: "Are you sure?"} %></span>
    </li>
  <% end %>
</ul>
<%= link_to '<<<< GO Back', articles_path %>
