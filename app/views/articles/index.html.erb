<h1>All Articles</h1>
<ul id="articles">
  <% @articles.each do |article| %>
    <li><%= link_to article.title, article_path(article), class: 'article_title' %></li>
  <% end %>
</ul>

<%= link_to 'New Article', new_article_path, class: 'new_article' %>

<%= link_to 'All Tags For this Blog', tags_path, class: 'all_tags' %>